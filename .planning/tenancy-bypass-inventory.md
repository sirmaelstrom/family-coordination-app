# Tenancy bypass inventory (fca-household-scope WP-02, quest ec788d69)

The discovery census that comes **before** the read switch (constraints M4, decisions D6). It classifies every place
in `src/FamilyCoordinationApp` where a household id meets a query, and every tenant read or write that can run with
no tenant set. WP-02 turns on the named `"Tenant"` global query filter; this file says, site by site, why each one
still works afterwards. WP-05 reruns it before removing the hand-written predicates.

**Classification** is exactly one of:
- `caller-scoped (no change)`: the Tenant filter agrees with the predicate already there, or no tenant set is read.
- `bypass: <gate>`: a legitimate cross-household read. It is `IgnoreQueryFilters(["Tenant"])` (plus `"SoftDelete"`
  where the site already ignored soft delete) with a `// TENANT-SCOPE-OK:` pragma naming the gate.
- `RunAs: <household source>`: system work with no caller. It runs inside `context.Tenant.RunAs(householdId)`.
- `AllowCrossTenantWrite: <reason>`: a write to another household's row. Inert until WP-03's write step.

**How the rows were found** (run from `src/FamilyCoordinationApp`, `Migrations/` excluded):
1. Every tenant comparison, both operand orders, **including caller-scoped ones** (163 hits before the switch):
   - `grep -rnE 'HouseholdId\w*\s*(==|!=)' . --include=*.cs --include=*.cshtml`
   - `grep -rnE '(==|!=)\s*[\w.?]*[Hh]ouseholdId' . --include=*.cs --include=*.cshtml`
2. A call-site pass over every call that passes a household argument **not** taken from the resolved caller
   (`user.`/`ctx.`/`caller.`): `grep -nE '[Hh]ouseholdId' Endpoints/*.cs` minus the caller-derived forms, then each
   service that calls another with a non-caller id (setup, approve/create, digest, the dev seed).
3. Every `TENANT-SCOPE-OK` pragma site (the fca#111 guard's 21).
4. Every tenant-set read or write reachable with no tenant: identity (the caller middleware's resolver, the whitelist
   handler, the dev bypass), the onboarding Razor Pages, the OAuth `OnCreatingTicket` event, the digest cron, the
   anonymous calendar feed, first-run setup, admin approve/create, and the dev seed. Found by reading every file
   that opens a context (`grep -rln 'IDbContextFactory<ApplicationDbContext>' .`) against the D3 marker map.

**Result.** No site outside the problem statement's known list (D6, D11) turned up, so E2 did not fire. Beyond the
list: `HouseholdMemberService` gains the pragma it never had, and `FeedbackService`'s author include is a bypass
(WP-02 step 5). `SetupService.GetUserByEmailAsync` has no caller, so it stays filtered and would fail loudly.

**Counts at the switch:** 200 rows: 23 `bypass:`, 23 `RunAs:`, 1 `AllowCrossTenantWrite:`, 153
`caller-scoped (no change)`. The census grep now has 165 hits: the 163 above, plus `GetConnectedRecipeAsync`'s
predicate (new, the 23rd bypass) and the filter's own doc comment in `ApplicationDbContext.cs`. The pre-switch
census (198 rows, 22 bypasses) is this file's first commit.

**Line numbers** point at the switch commit's tree. A `bypass:` row's line is the **first line of the statement**
holding the bypass, and guard fact 2 (`TenantFilterArchitectureTests`) holds those rows equal, `file:line` for
`file:line`, to the Tenant bypasses in `src`: a new bypass without a row, or a stale row, fails CI.

| file:line | expression (trimmed) | classification | gate / household source |
|---|---|---|---|
| `Authorization/DevAuthBypassMiddleware.cs:67` | `Users.OrderBy(u => u.Id).FirstOrDefaultAsync()` | bypass: dev-only seed identity | gated by `IsDevelopment()` at Authorization/DevAuthBypassMiddleware.cs:41 (registration is env-gated too; `DevAuthStartupGuard` fail-closes outside Development) |
| `Authorization/WhitelistedEmailHandler.cs:55` | `Users.AnyAsync(u => u.Email == email && u.IsWhitelisted)` | bypass: global whitelist check | the auth boundary: runs in `UseAuthorization`, before `CallerTenantMiddleware` sets a tenant; returns a boolean only |
| `Data/ApplicationDbContext.cs:112` | `/// <c>"Tenant"</c>: <c>e => BypassTenantFilter \|\| e.HouseholdId == CurrentHouseholdId</c>. It scopes reads,` | caller-scoped (no change) | not a query: the Tenant filter's own doc comment (`ApplyTenantFilter`) |
| `Data/SeedData.cs:18` | `context.Tenant.RunAs(household.Id)` from the `Users` lookup to the end of `SeedDevelopmentDataAsync` | RunAs: the dev household | the first `Households` row (not a tenant entity); covers the helpers' own contexts (shared scoped tenant) |
| `Data/SeedData.cs:20` | `var user = await context.Users.FirstOrDefaultAsync(u => u.HouseholdId == household.Id);` | RunAs: the dev household (`Households.FirstOrDefaultAsync()`) | inside `RunAs(household.Id)` at Data/SeedData.cs:18 |
| `Data/SeedData.cs:36` | `Recipes.AnyAsync()` (dev seed guard over the whole database) | bypass: dev seed whole-database guard | Development-only startup seed (Program.cs `IsDevelopment()` block); returns a boolean only |
| `Data/SeedData.cs:153` | `var alreadySeeded = await context.Rooms.AnyAsync(r => r.HouseholdId == householdId)` | RunAs: the household being seeded | the tenant in force: the enclosing `RunAs(newHouseholdId)` (SetupService, HouseholdRequestService approve/create, SeedDevelopmentDataAsync) or the Caller at Endpoints/ChoresEndpoints.cs:987 |
| `Data/SeedData.cs:154` | `\|\| await context.Chores.AnyAsync(c => c.HouseholdId == householdId);` | RunAs: the household being seeded | the tenant in force: the enclosing `RunAs(newHouseholdId)` (SetupService, HouseholdRequestService approve/create, SeedDevelopmentDataAsync) or the Caller at Endpoints/ChoresEndpoints.cs:987 |
| `Data/SeedData.cs:161` | `.Where(u => u.HouseholdId == householdId)` | RunAs: the household being seeded | the tenant in force: the enclosing `RunAs(newHouseholdId)` (SetupService, HouseholdRequestService approve/create, SeedDevelopmentDataAsync) or the Caller at Endpoints/ChoresEndpoints.cs:987 |
| `Data/SeedData.cs:361` | `var memberCount = await context.Users.CountAsync(u => u.HouseholdId == householdId);` | RunAs: the household being seeded | the tenant in force: the enclosing `RunAs` (SetupService, SeedDevelopmentDataAsync); soft-delete-only ignore (step 3) |
| `Data/SeedData.cs:365` | `.Where(cc => cc.HouseholdId == householdId)` | RunAs: the household being seeded | the tenant in force: the enclosing `RunAs(newHouseholdId)` (SetupService, HouseholdRequestService approve/create, SeedDevelopmentDataAsync) or the Caller at Endpoints/ChoresEndpoints.cs:987 |
| `Data/SeedData.cs:376` | `.Where(u => u.HouseholdId == householdId)` | RunAs: the household being seeded | the tenant in force: the enclosing `RunAs(newHouseholdId)` (SetupService, HouseholdRequestService approve/create, SeedDevelopmentDataAsync) or the Caller at Endpoints/ChoresEndpoints.cs:987 |
| `Data/SeedData.cs:390` | `.Where(u => u.HouseholdId == householdId && u.Email == email)` | RunAs: the household being seeded | the tenant in force: the enclosing `RunAs(newHouseholdId)` (SetupService, HouseholdRequestService approve/create, SeedDevelopmentDataAsync) or the Caller at Endpoints/ChoresEndpoints.cs:987 |
| `Data/SeedData.cs:418` | `.Where(c => c.HouseholdId == householdId)` | RunAs: the household being seeded | the tenant in force: the enclosing `RunAs(newHouseholdId)` (SetupService, HouseholdRequestService approve/create, SeedDevelopmentDataAsync) or the Caller at Endpoints/ChoresEndpoints.cs:987 |
| `Data/SeedData.cs:427` | `.Where(cc => cc.HouseholdId == householdId)` | RunAs: the household being seeded | the tenant in force: the enclosing `RunAs(newHouseholdId)` (SetupService, HouseholdRequestService approve/create, SeedDevelopmentDataAsync) or the Caller at Endpoints/ChoresEndpoints.cs:987 |
| `Data/SeedData.cs:477` | `.CountAsync(c => c.HouseholdId == householdId);` | RunAs: the household being seeded | the tenant in force: the enclosing `RunAs` (SetupService, SeedDevelopmentDataAsync); soft-delete-only ignore (step 3) |
| `Endpoints/CalendarTokenEndpoints.cs:96` | `tenant.RunAs(calendarToken.HouseholdId)` around the four board loads | RunAs: the token's household | the bypass-resolved token's `HouseholdId`; the scope wraps the whole `Task.WhenAll` (concurrent loads share the scoped tenant) |
| `Endpoints/CalendarTokenEndpoints.cs:99` | `GetBoardAsync(calendarToken.HouseholdId, …)` ×4 | caller-scoped (no change) | runs inside the feed's `RunAs(calendarToken.HouseholdId)` (row above), so the board's `householdId ==` predicates match the System tenant |
| `Endpoints/ChoresEndpoints.cs:576` | `.AnyAsync(c => c.HouseholdId == user.HouseholdId && c.ChoreId == choreId, ct);` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:648` | `var entity = await context.Users.FirstOrDefaultAsync(u => u.Id == userId && u.HouseholdId == householdId, ct);` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:702` | `var entity = await context.Users.FirstOrDefaultAsync(u => u.Id == userId && u.HouseholdId == householdId, ct);` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:745` | `.Where(u => u.HouseholdId == user.HouseholdId)` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:751` | `.Where(c => c.HouseholdId == user.HouseholdId)` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:756` | `.Where(c => c.HouseholdId == user.HouseholdId && c.Status == ChoreStatus.Active)` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:762` | `.Where(c => c.HouseholdId == user.HouseholdId)` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:766` | `.Where(r => r.HouseholdId == user.HouseholdId && !r.IsDeleted && r.CreatedByUserId != null)` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:770` | `.Where(s => s.HouseholdId == user.HouseholdId && s.IsManuallyAdded && s.AddedByUserId != null)` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:774` | `.Where(e => e.HouseholdId == user.HouseholdId)` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:778` | `.Where(m => m.HouseholdId == user.HouseholdId && m.CreatedByUserId != null)` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:785` | `.Where(u => u.HouseholdId == user.HouseholdId)` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:982` | `alreadyHad = await context.Rooms.AnyAsync(r => r.HouseholdId == user.HouseholdId, ct)` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/ChoresEndpoints.cs:983` | `\|\| await context.Chores.AnyAsync(c => c.HouseholdId == user.HouseholdId, ct);` | caller-scoped (no change) | `user.HouseholdId` / a `householdId` helper parameter fed `user.HouseholdId` |
| `Endpoints/MeEndpoints.cs:40` | `Users.Where(u => u.Email == email)` (/api/me) | caller-scoped (no change) | marked group: the middleware set Caller from this same email, so the row is in the Caller household; fca#111 pragma unchanged |
| `Endpoints/PresenceEndpoints.cs:54` | `Users.Where(u => u.Email == email)` (presence) | caller-scoped (no change) | marked group: the middleware set Caller from this same email, so the row is in the Caller household; fca#111 pragma unchanged |
| `Endpoints/RecipesEndpoints.cs:483` | `connected detail: `GetConnectedRecipeAsync(chId, recipeId)` (was `GetRecipeAsync(chId, …)`)` | caller-scoped (no change) | call site of the bypassing variant (the row below); gated by `AreHouseholdsConnectedAsync` at Endpoints/RecipesEndpoints.cs:477 |
| `Endpoints/RecipesEndpoints.cs:508` | `copy validation: `GetConnectedRecipeAsync(chId, recipeId)` (was `GetRecipeAsync(chId, …)`)` | caller-scoped (no change) | call site of the bypassing variant; gated by `AreHouseholdsConnectedAsync` at Endpoints/RecipesEndpoints.cs:502 |
| `Endpoints/RecipesEndpoints.cs:511` | `CopyRecipeFromConnectedHouseholdAsync(chId, recipeId, user.HouseholdId, …)` | caller-scoped (no change) | foreign `chId` flows to the copy-source bypasses in RecipeService (rows there); the write targets `user.HouseholdId` |
| `Endpoints/RecipesEndpoints.cs:644` | `.Where(r => r.HouseholdId == householdId && r.SourceUrl == url)` | caller-scoped (no change) | helper parameter fed `user.HouseholdId` at Endpoints/RecipesEndpoints.cs:385 |
| `Endpoints/ShoppingListEndpoints.cs:378` | `.Where(l => l.HouseholdId == householdId && l.ShoppingListId == listId)` | caller-scoped (no change) | `ctx.HouseholdId` (resolved caller) |
| `Endpoints/ShoppingListEndpoints.cs:406` | `i => i.HouseholdId == ctx.HouseholdId` | caller-scoped (no change) | `ctx.HouseholdId` (resolved caller) |
| `Endpoints/ShoppingListEndpoints.cs:496` | `i => i.HouseholdId == ctx.HouseholdId` | caller-scoped (no change) | `ctx.HouseholdId` (resolved caller) |
| `Endpoints/UploadsEndpoints.cs:106` | `route value `householdId` → `CanReadHouseholdUploadsAsync`` | caller-scoped (no change) | the route value reaches a query only through the rule-2 bypass (row at the `ImagePath == storedPath` line) |
| `Endpoints/UploadsEndpoints.cs:154` | `if (user.HouseholdId == householdId) return true;` | caller-scoped (no change) | in-memory rule 1: the caller's own household (no query) |
| `Endpoints/UploadsEndpoints.cs:173` | `.AnyAsync(r => r.HouseholdId == householdId && r.ImagePath == storedPath, ct);` | bypass: connected-household recipe image | gated by `AreHouseholdsConnectedAsync` at Endpoints/UploadsEndpoints.cs:167 plus the owning household's recipe referencing this exact file |
| `Endpoints/UserContextResolver.cs:32` | `Users.Where(u => u.Email == email)` | bypass: identity resolution | the query IS the scope source: the caller's own authenticated email claim; runs in `CallerTenantMiddleware` BEFORE the tenant exists, and in handlers under their own Caller |
| `Pages/Account/AccessDenied.cshtml.cs:37` | `Users.FirstOrDefaultAsync(u => u.Email == email)` | bypass: onboarding identity lookup | the query IS the scope source: the caller's own authenticated email claim; unmarked Razor Page (D3) |
| `Pages/Household/Pending.cshtml.cs:40` | `Users.FirstOrDefaultAsync(u => u.Email == email)` | bypass: onboarding identity lookup | the query IS the scope source: the caller's own authenticated email claim; unmarked Razor Page (D3) |
| `Pages/Household/Request.cshtml.cs:52` | `Users.FirstOrDefaultAsync(u => u.Email == UserEmail)` (GET) | bypass: onboarding identity lookup | the query IS the scope source: the caller's own authenticated email claim; unmarked Razor Page (D3) |
| `Pages/Household/Request.cshtml.cs:95` | `Users.FirstOrDefaultAsync(u => u.Email == UserEmail)` (POST) | bypass: onboarding identity lookup | the query IS the scope source: the caller's own authenticated email claim; unmarked Razor Page (D3) |
| `Services/Calendar/HouseholdCalendarTokenService.cs:29` | `SELECT pg_advisory_xact_lock({householdId})` (raw SQL) | caller-scoped (no change) | not a query over a tenant set; `householdId` from the marked management group; fca#111 pragma unchanged |
| `Services/Calendar/HouseholdCalendarTokenService.cs:32` | `.Where(calendarToken => calendarToken.HouseholdId == householdId && calendarToken.RevokedAt == null)` | caller-scoped (no change) | `householdId` from the marked management group (`user.HouseholdId`, CalendarTokenEndpoints) |
| `Services/Calendar/HouseholdCalendarTokenService.cs:58` | `.Where(calendarToken => calendarToken.HouseholdId == householdId && calendarToken.RevokedAt == null)` | caller-scoped (no change) | `householdId` from the marked management group (`user.HouseholdId`, CalendarTokenEndpoints) |
| `Services/Calendar/HouseholdCalendarTokenService.cs:74` | `HouseholdCalendarTokens.Where(t => t.TokenHash == hash && t.RevokedAt == null)` | bypass: calendar capability lookup | the token hash is the capability (a 256-bit secret, globally unique); called from the anonymous feed, unmarked |
| `Services/Calendar/HouseholdCalendarTokenService.cs:86` | `.Where(calendarToken => calendarToken.HouseholdId == householdId && calendarToken.RevokedAt == null)` | caller-scoped (no change) | `householdId` from the marked management group (`user.HouseholdId`, CalendarTokenEndpoints) |
| `Services/CategoryService.cs:18` | `.Where(c => c.HouseholdId == householdId);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/CategoryService.cs:36` | `.FirstOrDefaultAsync(c => c.HouseholdId == householdId && c.CategoryId == categoryId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/CategoryService.cs:52` | `.Where(c => c.HouseholdId == category.HouseholdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/CategoryService.cs:74` | `.FirstOrDefaultAsync(c => c.HouseholdId == category.HouseholdId && c.CategoryId == category.CategoryId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/CategoryService.cs:98` | `.FirstOrDefaultAsync(c => c.HouseholdId == householdId && c.CategoryId == categoryId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/CategoryService.cs:120` | `.FirstOrDefaultAsync(c => c.HouseholdId == householdId && c.CategoryId == categoryId && c.IsDeleted, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/CategoryService.cs:142` | `.FirstOrDefaultAsync(c => c.HouseholdId == householdId && c.CategoryId == categoryId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/CategoryService.cs:157` | `.Where(c => c.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/CategoryService.cs:168` | `.AnyAsync(i => i.HouseholdId == householdId && i.Category == categoryName, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreBoardService.cs:35` | `.Where(c => c.HouseholdId == householdId && c.Status == ChoreStatus.Active)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreBoardService.cs:39` | `.Where(r => r.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreBoardService.cs:45` | `.Where(u => u.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreBoardService.cs:55` | `.Where(u => u.HouseholdId == householdId && u.Id == userId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreBoardService.cs:73` | `.Where(c => c.HouseholdId == householdId && multiPersonIds.Contains(c.ChoreId))` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreBoardService.cs:87` | `.Where(e => e.HouseholdId == householdId && multiPersonIds.Contains(e.ChoreId))` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreBoardService.cs:99` | `.Where(s => s.HouseholdId == householdId && choreIds.Contains(s.ChoreId))` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreBoardService.cs:187` | `.Where(c => c.HouseholdId == householdId && c.Status == ChoreStatus.Active)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreRoomMembership.cs:32` | `.Where(cr => cr.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreRoomMembership.cs:54` | `.Where(cr => cr.HouseholdId == householdId && cr.ChoreId == choreId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreRoomMembership.cs:72` | `.Where(cr => cr.HouseholdId == householdId && cr.ChoreId == choreId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreService.cs:58` | `.Where(c => c.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreService.cs:199` | `.Where(cr => cr.HouseholdId == householdId && cr.ChoreId == choreId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreService.cs:341` | `.Where(cc => cc.HouseholdId == householdId && cc.ChoreId == choreId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreService.cs:420` | `.Where(s => s.HouseholdId == householdId && s.ChoreId == choreId && s.IsDone)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreService.cs:660` | `.FirstOrDefaultAsync(c => c.HouseholdId == householdId && c.ChoreId == choreId, cancellationToken)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreService.cs:667` | `.AnyAsync(u => u.Id == userId && u.HouseholdId == householdId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreService.cs:697` | `.Where(room => room.HouseholdId == householdId && distinct.Contains(room.RoomId))` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreSubtaskService.cs:35` | `.AnyAsync(c => c.HouseholdId == householdId && c.ChoreId == choreId, ct);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreSubtaskService.cs:42` | `.Where(s => s.HouseholdId == householdId && s.ChoreId == choreId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreSubtaskService.cs:121` | `.Where(s => s.HouseholdId == householdId && s.ChoreId == choreId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ChoreSubtaskService.cs:171` | `.FirstOrDefaultAsync(s => s.HouseholdId == householdId && s.ChoreId == choreId && s.SubtaskId == subtaskId, ct)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/DashboardService.cs:63` | `.Where(u => u.Id == userId && u.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/DashboardService.cs:77` | `.Where(mp => mp.HouseholdId == householdId && mp.WeekStartDate == weekStart)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/Digest/ChoreHistoryService.cs:150` | `.Where(u => u.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/ChoreHistoryService.cs:158` | `.Where(c => c.HouseholdId == householdId && c.CompletedAt >= oldestWeekStartUtc)` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/ChoreHistoryService.cs:165` | `.Where(c => c.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/ChoreHistoryService.cs:175` | `.Where(r => r.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/ChoreHistoryService.cs:188` | `.Where(c => c.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/ChoreHistoryService.cs:200` | `.Where(c => c.HouseholdId == householdId && c.Status == ChoreStatus.Active)` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/ChoreHistoryService.cs:210` | `.Where(c => c.HouseholdId == householdId && c.CompletedAt >= projectionReadStartUtc)` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/ChoreHistoryService.cs:227` | `.Where(e => e.HouseholdId == householdId && e.SetAt >= oldestWeekStartUtc)` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/ChoreHistoryService.cs:233` | `.Where(e => e.HouseholdId == householdId && e.SetAt < oldestWeekStartUtc)` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/ChoreHistoryService.cs:244` | `.Where(e => e.HouseholdId == householdId && e.SetAt < oldestWeekStartUtc && maxSetAts.Contains(e.SetAt))` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/ChoreRecapService.cs:70` | `.Where(h => h.Id == householdId)` | caller-scoped (no change) | `Households` is not a tenant entity; marked chores group (`user.HouseholdId`) |
| `Services/Digest/ChoreRecapService.cs:76` | `.Where(u => u.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/ChoreRecapService.cs:83` | `.Where(c => c.HouseholdId == householdId && c.CompletedAt >= oldestWeekStartUtc)` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/ChoreRecapService.cs:88` | `.Where(c => c.HouseholdId == householdId && c.Status == ChoreStatus.Active)` | caller-scoped (no change) | `householdId` from the marked chores group (`user.HouseholdId`, ChoresEndpoints recap/history) |
| `Services/Digest/DigestService.cs:55` | `ChoreDigestSettings.Where(s => s.Enabled && s.WebhookUrlProtected != null)` (all-households sweep) | bypass: digest all-households sweep | gated by `ValidateTriggerToken` (the cron's shared-secret header) at Endpoints/ChoresEndpoints.cs:1011; `HouseholdId` comes only from these rows |
| `Services/Digest/DigestService.cs:70` | `tenant.RunAs(candidate.HouseholdId)` around claim, build, decrypt, send and compensate | RunAs: the swept household | each candidate row's `HouseholdId` (D11) |
| `Services/Digest/DigestService.cs:111` | `.Where(s => s.HouseholdId == h && s.Enabled && s.WebhookUrlProtected != null` | RunAs: the swept household | inside the per-household `RunAs(candidate.HouseholdId)` at Services/Digest/DigestService.cs:70 |
| `Services/Digest/DigestService.cs:160` | `.Where(hh => hh.Id == householdId)` | caller-scoped (no change) | `Households` is not a tenant entity; `h` comes from the sweep rows |
| `Services/Digest/DigestService.cs:165` | `.Where(u => u.HouseholdId == householdId)` | RunAs: the swept household | inside the per-household `RunAs(candidate.HouseholdId)` at Services/Digest/DigestService.cs:70 |
| `Services/Digest/DigestService.cs:173` | `.Where(c => c.HouseholdId == householdId)` | RunAs: the swept household | inside the per-household `RunAs(candidate.HouseholdId)` at Services/Digest/DigestService.cs:70 |
| `Services/Digest/DigestService.cs:177` | `.Where(c => c.HouseholdId == householdId && c.Status == ChoreStatus.Active)` | RunAs: the swept household | inside the per-household `RunAs(candidate.HouseholdId)` at Services/Digest/DigestService.cs:70 |
| `Services/Digest/DigestService.cs:217` | `.Where(s => s.HouseholdId == householdId)` | RunAs: the swept household | inside the per-household `RunAs(candidate.HouseholdId)` at Services/Digest/DigestService.cs:70 |
| `Services/DigestSettingsService.cs:41` | `.FirstOrDefaultAsync(s => s.HouseholdId == householdId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/DigestSettingsService.cs:72` | `.FirstOrDefaultAsync(s => s.HouseholdId == householdId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/DigestSettingsService.cs:130` | `.FirstOrDefaultAsync(s => s.HouseholdId == householdId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/DraftService.cs:52` | `d.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/DraftService.cs:86` | `d.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/DraftService.cs:111` | `d.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/FeedbackService.cs:83` | `Feedbacks.AsNoTracking().Include(f => f.User)` | bypass: feedback authors across households | gated by `siteAdmin.IsSiteAdmin` at Endpoints/SettingsAdminEndpoints.cs:344; a non-admin keeps the `f.HouseholdId == householdId` predicate below (MN1). `Feedback` itself is not a tenant entity; the `User` include is |
| `Services/FeedbackService.cs:90` | `query = query.Where(f => f.HouseholdId == householdId);` | caller-scoped (no change) | `Feedback` is not a tenant entity (dual-mode, R-C1); the non-admin predicate stays (MN1) |
| `Services/FeedbackService.cs:117` | `Feedbacks.Where(f => f.Id == id)` (MutateAsync) | caller-scoped (no change) | `Feedback` is not a tenant entity and nothing is included, so no Tenant filter applies; fca#111 pragma unchanged |
| `Services/FeedbackService.cs:121` | `query = query.Where(f => f.HouseholdId == householdId);` | caller-scoped (no change) | `Feedback` is not a tenant entity (dual-mode, R-C1); the non-admin predicate stays (MN1) |
| `Services/HouseholdConnectionService.cs:82` | `.Where(i => i.HouseholdId == householdId && !i.IsUsed && i.ExpiresAt > DateTime.UtcNow)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/HouseholdConnectionService.cs:92` | `.Where(i => i.HouseholdId == householdId && !i.IsUsed && i.ExpiresAt > DateTime.UtcNow)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/HouseholdConnectionService.cs:124` | `HouseholdInvites.Include(i => i.Household).FirstOrDefaultAsync(i => i.InviteCode == normalizedCode)` (validate) | bypass: invite validate | the invite code is the capability (exact match, then `IsUsed`/`ExpiresAt`/self checks below it), rate-limited by `IsRateLimited` at Services/HouseholdConnectionService.cs:115 |
| `Services/HouseholdConnectionService.cs:148` | `if (invite.HouseholdId == acceptingHouseholdId)` | caller-scoped (no change) | in-memory self-connection guard on a bypass-loaded invite (no query) |
| `Services/HouseholdConnectionService.cs:180` | `HouseholdInvites.Include(i => i.Household).FirstOrDefaultAsync(i => i.InviteCode == normalizedCode)` (accept) | bypass: invite accept | the invite code is the capability (exact match, then `IsUsed`/`ExpiresAt`/self/already-connected checks below it) |
| `Services/HouseholdConnectionService.cs:194` | `if (invite.HouseholdId == acceptingHouseholdId)` | caller-scoped (no change) | in-memory self-connection guard on a bypass-loaded invite (no query) |
| `Services/HouseholdConnectionService.cs:222` | `context.Tenant.AllowCrossTenantWrite(...)` around the accept's `SaveChangesAsync` | AllowCrossTenantWrite: invite accept writes `UsedByHouseholdId` on the inviting household's row | the only `AllowCrossTenantWrite` site (D9); inert until WP-03 |
| `Services/HouseholdConnectionService.cs:251` | `.Where(c => c.HouseholdId1 == householdId \|\| c.HouseholdId2 == householdId)` | caller-scoped (no change) | `HouseholdConnection` is not a tenant entity (a pair, D5); ids are caller-derived |
| `Services/HouseholdConnectionService.cs:258` | `var otherHousehold = c.HouseholdId1 == householdId ? c.Household2 : c.Household1;` | caller-scoped (no change) | `HouseholdConnection` is not a tenant entity (a pair, D5); ids are caller-derived |
| `Services/HouseholdConnectionService.cs:277` | `.FirstOrDefaultAsync(c => c.HouseholdId1 == minId && c.HouseholdId2 == maxId, cancellationToken);` | caller-scoped (no change) | `HouseholdConnection` is not a tenant entity (a pair, D5); ids are caller-derived |
| `Services/HouseholdConnectionService.cs:306` | `.AnyAsync(c => c.HouseholdId1 == minId && c.HouseholdId2 == maxId, cancellationToken);` | caller-scoped (no change) | `HouseholdConnection` is not a tenant entity (a pair, D5); ids are caller-derived |
| `Services/HouseholdMemberService.cs:22` | `.Where(u => u.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/HouseholdMemberService.cs:36` | `.AnyAsync(u => u.Email == normalized && u.HouseholdId != householdId, cancellationToken);` | bypass: cross-household email uniqueness (returns only a boolean) | gated by the marked members group (Endpoints/SettingsEndpoints.cs:46); the read returns only the `OtherHousehold` outcome, never data |
| `Services/HouseholdMemberService.cs:45` | `.FirstOrDefaultAsync(u => u.Email == normalized && u.HouseholdId == householdId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/HouseholdMemberService.cs:86` | `.FirstOrDefaultAsync(u => u.HouseholdId == householdId && u.Id == targetUserId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/HouseholdMemberService.cs:97` | `.CountAsync(u => u.HouseholdId == householdId && u.IsWhitelisted, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/HouseholdMemberService.cs:121` | `.FirstOrDefaultAsync(u => u.HouseholdId == householdId && u.Id == targetUserId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/HouseholdMemberService.cs:128` | `.CountAsync(u => u.HouseholdId == householdId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/HouseholdRequestService.cs:33` | `Households.Include(h => h.Users)` (site-admin household list) | bypass: site-admin household list | gated by `RequireSiteAdmin` at Endpoints/SettingsAdminEndpoints.cs:77 (GetHouseholdRequests); needs every household's member count |
| `Services/HouseholdRequestService.cs:79` | `context.Tenant.RunAs(household.Id)` (approve) from the new id through the post-commit seed | RunAs: the household being approved | `household.Id` of the row this transaction just inserted (gate: `RequireSiteAdmin` at Endpoints/SettingsAdminEndpoints.cs:93) |
| `Services/HouseholdRequestService.cs:187` | `Users.AnyAsync(u => u.Email == email)` | bypass: cross-tenant email uniqueness (returns only a boolean) | gated by `RequireSiteAdmin` at Endpoints/SettingsAdminEndpoints.cs:150 (CreateHousehold); returns only a boolean |
| `Services/HouseholdRequestService.cs:205` | `context.Tenant.RunAs(household.Id)` (admin create) from the new id through the post-commit seed | RunAs: the household being created | `household.Id` of the row this transaction just inserted (gate: `RequireSiteAdmin` at Endpoints/SettingsAdminEndpoints.cs:150) |
| `Services/LoginProfileService.cs:42` | `Users.FirstOrDefaultAsync(u => u.Email == email)` | bypass: login-time identity lookup | the OAuth `OnCreatingTicket` event (Program.cs), unmarked; the principal's email was just authenticated by Google |
| `Services/LoginProfileService.cs:50` | `context.Tenant.RunAs(user.HouseholdId)` from the first mutation through `SaveChangesAsync` | RunAs: the signed-in user's household | the bypass-loaded user's own `HouseholdId` (D11); inert until WP-03's write step |
| `Services/MealPlanBoardService.cs:25` | `.Where(mp => mp.HouseholdId == householdId && mp.WeekStartDate == weekStart)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/MealPlanService.cs:19` | `.Where(mp => mp.HouseholdId == householdId && mp.WeekStartDate == weekStart)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/MealPlanService.cs:40` | `.Where(mp => mp.HouseholdId == householdId && mp.WeekStartDate == weekStart)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/MealPlanService.cs:95` | `e.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/MealPlanService.cs:168` | `e.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/MealPlanService.cs:181` | `.FirstOrDefaultAsync(mp => mp.HouseholdId == householdId && mp.MealPlanId == mealPlanId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/MealPlanService.cs:189` | `e.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/MealPlanService.cs:259` | `e.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/MealPlanService.cs:287` | `.FirstOrDefaultAsync(mp => mp.HouseholdId == householdId && mp.MealPlanId == mealPlanId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/MealPlanService.cs:307` | `e.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/MealPlanService.cs:337` | `.Where(mp => mp.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/MealPlanService.cs:346` | `.Where(e => e.HouseholdId == householdId && e.MealPlanId == mealPlanId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/MealPlanService.cs:355` | `.FirstOrDefaultAsync(mp => mp.HouseholdId == householdId && mp.MealPlanId == mealPlanId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/PresenceService.cs:106` | `_presence.Values.Where(p => p.HouseholdId == householdId && p.Status != PresenceStatus.Offline);` | caller-scoped (no change) | in-memory roster, not EF; `householdId` from the resolved caller |
| `Services/RecipeService.cs:19` | `.Where(r => r.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RecipeService.cs:44` | `.Where(r => r.HouseholdId == householdId && r.RecipeId == recipeId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RecipeService.cs:88` | `.FirstOrDefaultAsync(r => r.HouseholdId == recipe.HouseholdId && r.RecipeId == recipe.RecipeId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RecipeService.cs:160` | `.FirstOrDefaultAsync(r => r.HouseholdId == householdId && r.RecipeId == recipeId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RecipeService.cs:180` | `.Where(r => r.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RecipeService.cs:194` | `.Where(i => i.HouseholdId == householdId && i.Name.StartsWith(prefix))` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RecipeService.cs:207` | `.AnyAsync(f => f.UserId == userId && f.HouseholdId == householdId && f.RecipeId == recipeId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RecipeService.cs:214` | `.Where(f => f.UserId == userId && f.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RecipeService.cs:225` | `.FirstOrDefaultAsync(f => f.UserId == userId && f.HouseholdId == householdId && f.RecipeId == recipeId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RecipeService.cs:257` | `.Where(r => r.HouseholdId == connectedHouseholdId)` | bypass: connected-household recipe list | gated by `AreHouseholdsConnectedAsync` at Endpoints/RecipesEndpoints.cs:453 (ListConnectedRecipes, the only caller) |
| `Services/RecipeService.cs:284` | `.Where(r => r.HouseholdId == connectedHouseholdId && r.RecipeId == recipeId)` | bypass: connected-household recipe detail (`GetConnectedRecipeAsync`, new on `IRecipeService`) | gated by `AreHouseholdsConnectedAsync` at Endpoints/RecipesEndpoints.cs:477 and Endpoints/RecipesEndpoints.cs:502 (its only callers); the ordinary `GetRecipeAsync` stays filtered |
| `Services/RecipeService.cs:341` | `.Where(r => r.HouseholdId == sourceHouseholdId && r.RecipeId == sourceRecipeId)` | bypass: connected-household copy source | gated by `AreHouseholdsConnectedAsync` at Endpoints/RecipesEndpoints.cs:502 (CopyConnectedRecipe, the only caller) |
| `Services/RecipeService.cs:360` | `.Where(r => r.HouseholdId == sourceHouseholdId && r.RecipeId == sourceRecipeId)` | bypass: connected-household copy source | gated by `AreHouseholdsConnectedAsync` at Endpoints/RecipesEndpoints.cs:502 (CopyConnectedRecipe, the only caller) |
| `Services/RoomService.cs:18` | `.Where(r => r.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RoomService.cs:29` | `.FirstOrDefaultAsync(r => r.HouseholdId == householdId && r.RoomId == roomId, cancellationToken);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RoomService.cs:40` | `.Where(r => r.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RoomService.cs:44` | `.Where(r => r.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RoomService.cs:75` | `.FirstOrDefaultAsync(r => r.HouseholdId == householdId && r.RoomId == roomId, cancellationToken)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RoomService.cs:103` | `.FirstOrDefaultAsync(r => r.HouseholdId == householdId && r.RoomId == roomId, cancellationToken)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RoomService.cs:112` | `.Where(cr => cr.HouseholdId == householdId && cr.RoomId == roomId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/RoomService.cs:135` | `.Where(r => r.HouseholdId == householdId && orderedRoomIds.Contains(r.RoomId))` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/SetupService.cs:55` | `Users.FirstOrDefaultAsync(u => u.Email == userEmail)` | bypass: first-run setup identity check | gated by `IsSetupCompleteAsync` at Pages/Setup/FirstRunSetup.cshtml.cs:52 (setup refuses once any household exists); returns only whether the email exists |
| `Services/SetupService.cs:74` | `context.Tenant.RunAs(household.Id)` from the new household's id through both seed calls | RunAs: the household being created | `household.Id` of the row this method just inserted; covers the Users save, `SeedDefaultCategoriesAsync` and `SeedChoresAndRoomsAsync` (their own contexts share the scoped tenant) |
| `Services/SetupService.cs:109` | `Users.Include(u => u.Household).FirstOrDefaultAsync(u => u.Email == email)` (`GetUserByEmailAsync`) | caller-scoped (no change) | no caller in `src` or `tests`: left filtered so a future unmarked caller fails loudly (D12); its fca#111 pragma is unchanged (MN1) |
| `Services/ShoppingListGenerator.cs:66` | `.Where(mp => mp.HouseholdId == householdId && mp.MealPlanId == mealPlanId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListGenerator.cs:158` | `.Where(sl => sl.HouseholdId == householdId && sl.ShoppingListId == shoppingListId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListGenerator.cs:179` | `.Where(mp => mp.HouseholdId == householdId && mp.MealPlanId == existingList.MealPlanId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:22` | `.Where(sl => sl.HouseholdId == householdId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:52` | `.Where(sl => sl.HouseholdId == householdId && sl.ShoppingListId == shoppingListId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:64` | `.Where(sl => sl.HouseholdId == householdId && !sl.IsArchived)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:76` | `.Where(sl => sl.HouseholdId == householdId && sl.IsArchived);` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:95` | `.FirstOrDefaultAsync(sl => sl.HouseholdId == householdId && sl.ShoppingListId == shoppingListId, cancellationToken)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:111` | `.FirstOrDefaultAsync(sl => sl.HouseholdId == householdId && sl.ShoppingListId == shoppingListId, cancellationToken)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:131` | `.FirstOrDefaultAsync(sl => sl.HouseholdId == householdId && sl.ShoppingListId == shoppingListId, cancellationToken)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:151` | `.Where(i => i.HouseholdId == item.HouseholdId && i.ShoppingListId == item.ShoppingListId)` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:192` | `i.HouseholdId == item.HouseholdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:292` | `i.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:314` | `sl.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:341` | `.Where(i => i.HouseholdId == householdId && i.Name.ToLower().StartsWith(lower))` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:356` | `.Where(i => i.HouseholdId == householdId` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:375` | `sl.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:396` | `i.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
| `Services/ShoppingListService.cs:419` | `i.HouseholdId == householdId &&` | caller-scoped (no change) | `householdId` parameter; every caller passes the resolved caller's household from a marked group (call-site pass) |
