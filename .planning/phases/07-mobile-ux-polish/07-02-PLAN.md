---
phase: 07-mobile-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Components/Pages/ShoppingList.razor
  - Components/Shared/ShoppingItemRow.razor
  - wwwroot/css/app.css
autonomous: true

must_haves:
  truths:
    - "Shopping list checkboxes have minimum 40px touch target"
    - "All interactive elements are easily tappable on mobile"
    - "No touch targets smaller than 40px on critical paths"
  artifacts: []
  key_links: []
---

<objective>
Ensure all interactive elements meet 40px minimum touch target size, with focus on shopping list checkboxes.

Purpose: Make the app usable at the grocery store with one hand, potentially while holding items.
Output: Touch-optimized UI components.
</objective>

<execution_context>
@/home/[USER]/.claude/get-shit-done/workflows/execute-plan.md
@/home/[USER]/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-mobile-ux-polish/07-CONTEXT.md
@.planning/phases/07-mobile-ux-polish/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Audit shopping list touch targets</name>
  <files>src/FamilyCoordinationApp/Components/</files>
  <action>
    Review ShoppingList.razor and related components.
    Identify all interactive elements:
    - Checkboxes (primary concern)
    - Edit/delete buttons
    - Category expand/collapse
    - Add item button
    
    Note current sizes and what needs adjustment.
  </action>
  <verify>List of elements needing size adjustment documented</verify>
  <done>Touch target audit complete</done>
</task>

<task type="auto">
  <name>Increase checkbox touch targets</name>
  <files>src/FamilyCoordinationApp/Components/Pages/ShoppingList.razor, src/FamilyCoordinationApp/Components/Shared/ShoppingItemRow.razor</files>
  <action>
    For MudCheckBox components:
    1. Add Size="Size.Large" parameter
    2. If needed, wrap in container with min-width/min-height CSS
    
    For custom checkboxes:
    Add CSS class with minimum 44px touch target.
    
    Ensure the entire row is tappable (not just the checkbox icon).
  </action>
  <verify>Checkboxes visually larger, tap area increased</verify>
  <done>Shopping list checkboxes meet 40px minimum</done>
</task>

<task type="auto">
  <name>Optimize action buttons</name>
  <files>src/FamilyCoordinationApp/Components/Pages/ShoppingList.razor</files>
  <action>
    For icon buttons (edit, delete, etc.):
    - Use Size="Size.Medium" or Size="Size.Large" on MudIconButton
    - Ensure adequate spacing between adjacent buttons (8px minimum)
    
    For the FAB (add item):
    - Already large, verify it's not obscured on mobile
  </action>
  <verify>Action buttons are easily tappable</verify>
  <done>Action buttons optimized for touch</done>
</task>

<task type="auto">
  <name>Add CSS overrides for mobile</name>
  <files>src/FamilyCoordinationApp/wwwroot/css/app.css</files>
  <action>
    Add mobile-specific CSS:
    ```css
    /* Touch target optimization */
    @media (max-width: 600px) {
      .shopping-item .mud-checkbox {
        min-width: 44px;
        min-height: 44px;
      }
      
      .shopping-item {
        padding: 8px 0;
        min-height: 48px;
      }
      
      .mud-icon-button {
        min-width: 40px;
        min-height: 40px;
      }
    }
    ```
    
    Adjust class names to match actual component structure.
  </action>
  <verify>CSS added with mobile media query</verify>
  <done>Mobile touch targets enforced via CSS</done>
</task>

</tasks>

<verification>
1. Run app in mobile viewport (Chrome DevTools, 375px width)
2. Use "Show tap targets" in DevTools if available
3. Manually verify checkboxes are easy to tap
4. Test on actual phone if available
</verification>

<success_criteria>
- All checkboxes have 40px+ touch targets
- Action buttons are not cramped together
- Shopping list is usable with one hand on phone
- No accidental mis-taps on adjacent elements
</success_criteria>

<output>
After completion, create `.planning/phases/07-mobile-ux-polish/07-02-SUMMARY.md`
</output>
